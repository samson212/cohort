#!/bin/bash
# cohort-push: push the current branch with upstream tracking.
# Called by /git-push after sync/confirm steps.
# Usage: cohort-push
set -e

cd "$(git rev-parse --show-toplevel)"
branch=$(git branch --show-current)
git push -u origin "$branch"

#!/bin/bash
# cohort-model-name: print the active model name for commit trailers.
# Pulls from SHELLEY_MODEL env var.
set -e

if [[ -n "$SHELLEY_MODEL" ]]; then
    printf '%s\n' "$SHELLEY_MODEL"
else
    echo "unknown-model"
fi
